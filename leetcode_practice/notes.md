# Two Sum
Pattern:
Use hashmap for O(1) lookup.

# Contains Duplicate
Use set to detect duplicates quickly.

Today I learned:
- hashmap lookup
- two pointer basics
- array traversal

# Best Time to Buy and Sell Stock

Pattern:
Track minimum value while traversing array.

Logic:
At each step calculate possible profit.

Why:
Avoid nested loops and achieve O(n).

# Contains Duplicate

Pattern:
Use set for fast lookup.

Logic:
If number already exists in set -> duplicate found.

Time Complexity:
O(n)

Why better:
Avoids nested loops.